# AirGapper

Build a simple, fully offline screen-to-camera file and text transfer tool, primarily for an older Android receiver with no network connection.

- Keep the app self-contained in root `index.html`.
- Do not add or run tests, verification, build tooling, or documentation.
- Optimize for fast iteration: make minimal, direct code changes.
- After changes, commit and push.

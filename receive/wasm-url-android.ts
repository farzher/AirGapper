// The APK uses the decoder artifact from the last known-working Android
// release. Keeping it separate leaves the optimized browser decoder intact.
import dataUrl from "virtual:android-codec-wasm-data-url";

export default dataUrl;

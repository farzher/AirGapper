// Canonical AirGrid runtime entry. Keep the public page URL stable while
// experimental implementations can still live in versioned source files.
await import('./airgrid-lab-1430.js?rev=canonical-1430');

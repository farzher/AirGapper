import { createServer } from 'node:http';
import { spawn, spawnSync } from 'node:child_process';
import { existsSync } from 'node:fs';
import { mkdtemp, readFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

const testsDir=dirname(fileURLToPath(import.meta.url));
const root=resolve(testsDir,'..');
const hardwareBaseline=JSON.parse(await readFile(join(testsDir,'fixtures','airgrid-x1','phone-v35b-quick-a.json'),'utf8'));
const hardwareFollowup=JSON.parse(await readFile(join(testsDir,'fixtures','airgrid-x1','phone-v35f-quick-a.json'),'utf8'));
const hardwareCurrent=JSON.parse(await readFile(join(testsDir,'fixtures','airgrid-x1','phone-v35h-quick-a.json'),'utf8'));
const hardwareLatest=JSON.parse(await readFile(join(testsDir,'fixtures','airgrid-x1','phone-v35i-quick-a.json'),'utf8'));
const hardwareNewest=JSON.parse(await readFile(join(testsDir,'fixtures','airgrid-x1','phone-v35j-quick-a.json'),'utf8'));
const hardwareNative=JSON.parse(await readFile(join(testsDir,'fixtures','airgrid-x1','phone-v35l-quick-a.json'),'utf8'));
const hardwareMeasured=JSON.parse(await readFile(join(testsDir,'fixtures','airgrid-x1','phone-v35m-quick-a.json'),'utf8'));
const files=new Map([
  ['/',{type:'text/html; charset=utf-8',body:await readFile(join(root,'index.html'))}],
  ['/index.html',{type:'text/html; charset=utf-8',body:await readFile(join(root,'index.html'))}],
  ['/airgrid-x1.js',{type:'text/javascript; charset=utf-8',body:await readFile(join(root,'airgrid-x1.js'))}],
]);
const server=createServer((req,res)=>{const item=files.get((req.url||'').split('?')[0]);if(!item){res.writeHead(404);res.end('not found');return;}res.writeHead(200,{'content-type':item.type,'cache-control':'no-store'});res.end(item.body);});
await new Promise(resolve=>server.listen(0,'127.0.0.1',resolve));
const httpPort=server.address().port;
const debugPort=9800+Math.floor(Math.random()*500);
const profile=await mkdtemp(join(tmpdir(),'airgapper-x1-benchmark-'));
const chrome=process.env.CHROME_PATH||[
  'C:/Program Files/Google/Chrome/Application/chrome.exe',
  'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',
  '/usr/bin/google-chrome','/usr/bin/chromium','/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'
].find(path=>existsSync(path));
if(!chrome)throw new Error('Chrome not found. Set CHROME_PATH.');
const child=spawn(chrome,['--headless=new','--disable-gpu','--no-first-run','--no-default-browser-check',`--remote-debugging-port=${debugPort}`,`--user-data-dir=${profile}`,'--window-size=1280,900','about:blank'],{stdio:'ignore'});
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function waitForChrome(){for(let i=0;i<100;i++){try{const r=await fetch(`http://127.0.0.1:${debugPort}/json/version`);if(r.ok)return;}catch{}await sleep(100);}throw new Error('Chrome DevTools endpoint did not start');}
class Cdp{
  constructor(ws){this.ws=ws;this.seq=0;this.pending=new Map();ws.onmessage=event=>{const m=JSON.parse(event.data);if(!m.id)return;const p=this.pending.get(m.id);if(!p)return;this.pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result);};}
  static async connect(url){const ws=new WebSocket(url);await new Promise((resolve,reject)=>{ws.onopen=resolve;ws.onerror=reject;});return new Cdp(ws);}
  send(method,params={}){return new Promise((resolve,reject)=>{const id=++this.seq;this.pending.set(id,{resolve,reject});this.ws.send(JSON.stringify({id,method,params}));});}
  close(){this.ws.close();}
}

let cdp;
try{
  await waitForChrome();
  const target=await (await fetch(`http://127.0.0.1:${debugPort}/json/new?${encodeURIComponent(`http://127.0.0.1:${httpPort}/`)}`,{method:'PUT'})).json();
  cdp=await Cdp.connect(target.webSocketDebuggerUrl);
  await cdp.send('Runtime.enable');
  await sleep(500);
  const expression=String.raw`(async()=>{
    const wait=ms=>new Promise(r=>setTimeout(r,ms));
    for(let i=0;i<100&&!window.AirGridX1;i++)await wait(50);
    if(!window.AirGridX1)throw new Error('AirGridX1 did not load');
    const A=AirGridX1,renderer=A.createWorker(),workers=[];
    const worker=()=>{const w=A.createWorker();workers.push(w);return w;};
    async function render({profile='M1',pitch=3,cols=3,rows=2,frame=77,pageFps=0}={}){const r=await A.request(renderer,'render',{config:{profile,pitch,cols,rows,frame,pageFps,session:0x13579bdf}}),c=document.createElement('canvas');c.width=r.width;c.height=r.height;c.getContext('2d',{alpha:false}).drawImage(r.bitmap,0,0);r.bitmap.close();return{canvas:c,meta:r};}
    async function decode(canvas,w=worker(),options={track:false}){const bitmap=await createImageBitmap(canvas),r=await A.request(w,'decode',{bitmap,options},[bitmap]);return r.result;}
    function i420Frame(canvas){const w=canvas.width,h=canvas.height,rgba=canvas.getContext('2d',{willReadFrequently:true}).getImageData(0,0,w,h).data,uw=(w+1)>>1,uh=(h+1)>>1,ySize=w*h,uvSize=uw*uh,buf=new Uint8Array(ySize+uvSize*2),clamp=x=>Math.max(0,Math.min(255,Math.round(x)));for(let y=0;y<h;y++)for(let x=0;x<w;x++){const i=(y*w+x)*4;buf[y*w+x]=clamp(16+.257*rgba[i]+.504*rgba[i+1]+.098*rgba[i+2]);}for(let y=0;y<uh;y++)for(let x=0;x<uw;x++){let r=0,g=0,b=0,n=0;for(let dy=0;dy<2;dy++)for(let dx=0;dx<2;dx++){const sx=x*2+dx,sy=y*2+dy;if(sx>=w||sy>=h)continue;const i=(sy*w+sx)*4;r+=rgba[i];g+=rgba[i+1];b+=rgba[i+2];n++;}r/=n;g/=n;b/=n;const p=y*uw+x;buf[ySize+p]=clamp(128-.148*r-.291*g+.439*b);buf[ySize+uvSize+p]=clamp(128+.439*r-.368*g-.071*b);}return new VideoFrame(buf,{format:'I420',codedWidth:w,codedHeight:h,timestamp:0});}
    async function decodeI420(canvas,w=worker(),options={track:false,detectorLongSide:768}){const frame=i420Frame(canvas),r=await A.request(w,'decode',{bitmap:frame,options:{...options,detectorPipeline:'i420'}},[frame]);return r.result;}
    function clone(source){const c=document.createElement('canvas');c.width=source.width;c.height=source.height;c.getContext('2d',{alpha:false}).drawImage(source,0,0);return c;}
    function transformed(source,{mirror=false,rotate=0,blur=0,moire=0}={}){const c=document.createElement('canvas'),swap=Math.abs(rotate)%180===90;c.width=swap?source.height:source.width;c.height=swap?source.width:source.height;const x=c.getContext('2d',{alpha:false});x.fillStyle='#000';x.fillRect(0,0,c.width,c.height);x.translate(c.width/2,c.height/2);x.rotate(rotate*Math.PI/180);x.scale(mirror?-1:1,1);x.filter=blur?'blur('+blur+'px)':'none';x.drawImage(source,-source.width/2,-source.height/2);x.setTransform(1,0,0,1,0,0);x.filter='none';if(moire){x.globalAlpha=moire;x.fillStyle='#fff';for(let xx=1;xx<c.width;xx+=4)x.fillRect(xx,0,1,c.height);x.globalAlpha=moire*.7;for(let yy=2;yy<c.height;yy+=7)x.fillRect(0,yy,c.width,1);x.globalAlpha=1;}return c;}
    function perspective(source){const pad=28,c=document.createElement('canvas');c.width=source.width+pad*2;c.height=source.height+pad*2;const out=c.getContext('2d',{willReadFrequently:true}),src=source.getContext('2d').getImageData(0,0,source.width,source.height),dst=out.createImageData(c.width,c.height),q=[[20,12],[c.width-34,25],[c.width-8,c.height-18],[38,c.height-5]];for(let y=0;y<c.height;y++){const v=Math.max(0,Math.min(1,(y-12)/(c.height-24))),lx=q[0][0]+(q[3][0]-q[0][0])*v,rx=q[1][0]+(q[2][0]-q[1][0])*v;for(let x=Math.ceil(lx);x<=Math.floor(rx);x++){const u=(x-lx)/(rx-lx),sx=Math.max(0,Math.min(source.width-1,Math.round(u*(source.width-1)))),sy=Math.max(0,Math.min(source.height-1,Math.round(v*(source.height-1)))),si=(sy*source.width+sx)*4,di=(y*c.width+x)*4;dst.data[di]=src.data[si];dst.data[di+1]=src.data[si+1];dst.data[di+2]=src.data[si+2];dst.data[di+3]=255;}}out.putImageData(dst,0,0);return c;}
    function channelShift(source,{exposure=.82,mix=.12}={}){const c=clone(source),x=c.getContext('2d',{willReadFrequently:true}),d=x.getImageData(0,0,c.width,c.height),a=d.data;for(let i=0;i<a.length;i+=4){const r=a[i],g=a[i+1],b=a[i+2];a[i]=Math.max(0,Math.min(255,(r*(1-mix)+g*mix)*exposure+9));a[i+1]=Math.max(0,Math.min(255,(g*(1-mix)+b*mix)*exposure+5));a[i+2]=Math.max(0,Math.min(255,(b*(1-mix)+r*mix)*exposure+13));}x.putImageData(d,0,0);return c;}
    function cameraResample(source,scale=.48){const c=document.createElement('canvas');c.width=Math.ceil(source.width*scale+1);c.height=Math.ceil(source.height*scale+1);const x=c.getContext('2d',{alpha:false});x.fillStyle='#000';x.fillRect(0,0,c.width,c.height);x.imageSmoothingEnabled=true;x.imageSmoothingQuality='high';x.setTransform(scale,0,0,scale,.37,.29);x.drawImage(source,0,0);return c;}
    function compact(name,r){return{name,detector:r.detectorSize,fiducials:r.fiducials,candidates:r.candidates,pipeline:r.detectorPipeline,pipelineFallback:r.detectorFallback,sourceFormat:r.sourceFormat,neighborChecks:r.markerNeighborChecks,headerTimingPhases:r.headerTimingPhases,headerFullPhases:r.headerFullPhases,tracked:r.tracked,complete:r.completeTiles,headers:r.headerOk,payloads:r.payloadOk,frames:r.frames,ber:r.metrics.rawBer,miChip:r.metrics.mutualInformationPerChip,verified:r.metrics.verifiedUniqueBytes,projected:r.metrics.projectedFullGridValidatedBytes,timings:r.timings,tiles:r.tiles.map(t=>({x:t.header.tileX,y:t.header.tileY,frame:t.header.frame,flags:t.header.flags,ok:t.payloadOk,rawErrors:t.rawErrors,eccIterations:t.eccIterations}))};}
    const base=await render(),results=[];
    results.push(compact('clean-full',await decode(base.canvas)));
    const p=3,m=4*p,crop=document.createElement('canvas');crop.width=(64+8)*p;crop.height=(64+8)*p;crop.getContext('2d').drawImage(base.canvas,-(m+64*p-4*p),-(m-4*p));results.push(compact('arbitrary-one-tile-crop',await decode(crop)));
    const crop2=document.createElement('canvas');crop2.width=(128+8)*p;crop2.height=(64+8)*p;crop2.getContext('2d').drawImage(base.canvas,-(m-4*p),-(m-4*p));results.push(compact('multiple-tile-crop',await decode(crop2)));
    results.push(compact('perspective',await decode(perspective(base.canvas))));
    results.push(compact('rotation-90',await decode(transformed(base.canvas,{rotate:90}))));
    results.push(compact('mirrored',await decode(transformed(base.canvas,{mirror:true}))));
    results.push(compact('blur',await decode(transformed(base.canvas,{blur:.55}))));
    results.push(compact('moire',await decode(transformed(base.canvas,{moire:.10}))));
    results.push(compact('exposure-white-balance-channel-mix',await decode(channelShift(base.canvas))));
    const cameraBase=await render({pitch:4});results.push(compact('camera-resample-p4',await decode(cameraResample(cameraBase.canvas))));results.push(compact('camera-dim-resample-p4',await decode(cameraResample(channelShift(cameraBase.canvas,{exposure:.6,mix:.08})))));
    const frameB=await render({frame:78}),rolling=clone(base.canvas),rx=rolling.getContext('2d'),split=Math.floor(rolling.height/2);rx.drawImage(frameB.canvas,0,split,frameB.canvas.width,frameB.canvas.height-split,0,split,rolling.width,rolling.height-split);results.push(compact('rolling-shutter-composite',await decode(rolling)));
    const badHeader=clone(base.canvas),bh=badHeader.getContext('2d');for(let ty=0;ty<2;ty++)for(let tx=0;tx<3;tx++){bh.fillStyle='#000';bh.fillRect(m+(tx*64+8)*p,m+(ty*64+7)*p,48*p,8*p);}results.push(compact('corrupted-headers',await decode(badHeader)));
    const badPayload=clone(base.canvas),bp=badPayload.getContext('2d');bp.fillStyle='#fff';for(let ty=0;ty<2;ty++)for(let tx=0;tx<3;tx++)bp.fillRect(m+(tx*64+8)*p,m+(ty*64+23)*p,48*p,40*p);results.push(compact('corrupted-payloads',await decode(badPayload)));
    const noise=document.createElement('canvas');noise.width=base.canvas.width;noise.height=base.canvas.height;const nx=noise.getContext('2d'),nd=nx.createImageData(noise.width,noise.height);let seed=0x6d2b79f5;for(let i=0;i<nd.data.length;i+=4){seed^=seed<<13;seed^=seed>>>17;seed^=seed<<5;const v=seed&127;nd.data[i]=v;nd.data[i+1]=v;nd.data[i+2]=v;nd.data[i+3]=255;}nx.putImageData(nd,0,0);results.push(compact('false-positive-noise',await decode(noise)));
    const profileResults=[];for(const profile of['binary','M2','M3']){const r=await render({profile,cols:1,rows:1});profileResults.push(compact('clean-'+profile,await decode(r.canvas)));}
    const dense=await render({profile:'M1',pitch:2,cols:10,rows:5,frame:88,pageFps:5});results.push(compact('dense-hardware-grid',await decode(dense.canvas)));results.push(compact('dense-hardware-grid-d640',await decode(dense.canvas,worker(),{track:false,detectorLongSide:640})));results.push(compact('dense-hardware-grid-i420-d768',await decodeI420(dense.canvas)));
    const tw=worker(),trackA=await decode(base.canvas,tw,{track:true}),trackBFrame=await render({frame:79}),trackB=await decode(trackBFrame.canvas,tw,{track:true});
    const taggedFrame=await render({profile:'M2',pitch:4,cols:1,rows:1,frame:91,pageFps:15}),hardwareTag=compact('hardware-tag',await decode(taggedFrame.canvas));
    const st=await A.request(worker(),'selftest');
    localStorage.setItem('airgridX1HardwareResults',JSON.stringify({schema:2,build:'v35e',startedAt:'stale',conditions:{stale:{}}}));history.replaceState(null,'','/?airgridLab=1&role=receiver');await A.startLab();const labSmoke={workerBuild:A.build,versionText:document.querySelector('.version')?.textContent||'',autoSender:!!document.querySelector('#x1Auto'),autoReceiver:!!document.querySelector('#x1Camera'),recordQuick:!!document.querySelector('#x1Record'),saveVideo:!!document.querySelector('#x1SaveVideo'),receiverVisible:getComputedStyle(document.querySelector('#x1Camera')).display!=='none',recordVisible:getComputedStyle(document.querySelector('#x1Record')).display!=='none',senderHidden:getComputedStyle(document.querySelector('#x1Auto')).display==='none',exportJson:!!document.querySelector('#x1Export'),exportCsv:!!document.querySelector('#x1Csv'),staleResultsCleared:!localStorage.getItem('airgridX1HardwareResults'),quickSweepText:document.querySelector('#x1Sweep')?.selectedOptions[0]?.textContent||'',quickDwell:document.querySelector('#x1Dwell')?.value,detectorMode:document.querySelector('#x1Detector')?.value,detectorOptions:[...document.querySelector('#x1Detector')?.options||[]].map(x=>x.value),pipelineMode:document.querySelector('#x1Pipeline')?.value,pipelineOptions:[...document.querySelector('#x1Pipeline')?.options||[]].map(x=>x.value)};
    for(const w of workers)w.terminate();renderer.terminate();
    return{format:A.format,seed:'0x6d2b79f5',results,profileResults,hardwareTag,labSmoke,tracking:{first:compact('tracking-acquire',trackA),second:compact('tracking-follow',trackB)},selftest:st.result};
  })()`;
  const evaluated=await cdp.send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true});
  if(evaluated.exceptionDetails)throw new Error(evaluated.exceptionDetails.exception?.description||evaluated.exceptionDetails.text);
  const report=evaluated.result.value;report.physicalBaseline={build:hardwareBaseline.build,usefulFps:hardwareBaseline.scanSummary?.usefulFps,workerUtilization:hardwareBaseline.scanSummary?.workerUtilization,conditions:Object.keys(hardwareBaseline.conditions||{}).length,m1Pitch4Frozen:hardwareBaseline.conditions?.['M1-p4-f0']?.metrics};const followupConditions=Object.values(hardwareFollowup.conditions||{});report.physicalFollowup={build:hardwareFollowup.build,usefulFps:hardwareFollowup.scanSummary?.usefulFps,workerUtilization:hardwareFollowup.scanSummary?.workerUtilization,conditions:followupConditions.length,completeTiles:followupConditions.reduce((n,c)=>n+c.completeTiles,0),payloadOkTiles:followupConditions.reduce((n,c)=>n+c.payloadOkTiles,0),persistentCameraIds:!!(hardwareFollowup.camera?.deviceId||hardwareFollowup.camera?.groupId||hardwareFollowup.cameraCapabilities?.deviceId||hardwareFollowup.cameraCapabilities?.groupId)};const currentConditions=Object.values(hardwareCurrent.conditions||{});report.physicalCurrent={build:hardwareCurrent.build,startedAt:hardwareCurrent.startedAt,expectedConditions:hardwareCurrent.expectedConditions,conditions:currentConditions.length,completeTiles:currentConditions.reduce((n,c)=>n+c.completeTiles,0),payloadOkTiles:currentConditions.reduce((n,c)=>n+c.payloadOkTiles,0),persistentCameraIds:!!(hardwareCurrent.camera?.deviceId||hardwareCurrent.camera?.groupId||hardwareCurrent.cameraCapabilities?.deviceId||hardwareCurrent.cameraCapabilities?.groupId)};const latestConditions=Object.values(hardwareLatest.conditions||{});report.physicalLatest={build:hardwareLatest.build,startedAt:hardwareLatest.startedAt,expectedConditions:hardwareLatest.expectedConditions,conditions:latestConditions.length,usefulFps:hardwareLatest.scanSummary?.usefulFps,completeTiles:latestConditions.reduce((n,c)=>n+c.completeTiles,0),payloadOkTiles:latestConditions.reduce((n,c)=>n+c.payloadOkTiles,0),persistentCameraIds:!!(hardwareLatest.camera?.deviceId||hardwareLatest.camera?.groupId||hardwareLatest.cameraCapabilities?.deviceId||hardwareLatest.cameraCapabilities?.groupId)};const newestConditions=Object.values(hardwareNewest.conditions||{});report.physicalNewest={build:hardwareNewest.build,startedAt:hardwareNewest.startedAt,expectedConditions:hardwareNewest.expectedConditions,detectorMode:hardwareNewest.detectorMode,detectorVariants:Object.keys(hardwareNewest.detectorVariants||{}),conditions:newestConditions.length,usefulFps:hardwareNewest.scanSummary?.usefulFps,completeTiles:newestConditions.reduce((n,c)=>n+c.completeTiles,0),payloadOkTiles:newestConditions.reduce((n,c)=>n+c.payloadOkTiles,0),persistentCameraIds:!!(hardwareNewest.camera?.deviceId||hardwareNewest.camera?.groupId||hardwareNewest.cameraCapabilities?.deviceId||hardwareNewest.cameraCapabilities?.groupId)};const nativeConditions=Object.values(hardwareNative.conditions||{}),nativeVariant=hardwareNative.detectorVariants?.['i420-d768'];report.physicalNative={build:hardwareNative.build,startedAt:hardwareNative.startedAt,usefulFps:hardwareNative.scanSummary?.usefulFps,detectorPipeline:nativeVariant?.detectorPipeline,detectorFallback:nativeVariant?.detectorFallback,sourceFormat:nativeVariant?.sourceFormat,scanFrames:nativeVariant?.scanFrames,completeTiles:nativeConditions.reduce((n,c)=>n+c.completeTiles,0),payloadOkTiles:nativeConditions.reduce((n,c)=>n+c.payloadOkTiles,0),persistentCameraIds:!!(hardwareNative.camera?.deviceId||hardwareNative.camera?.groupId||hardwareNative.cameraCapabilities?.deviceId||hardwareNative.cameraCapabilities?.groupId)};const measuredConditions=Object.values(hardwareMeasured.conditions||{}),measuredVariant=hardwareMeasured.detectorVariants?.['i420-d768'];report.physicalMeasured={build:hardwareMeasured.build,startedAt:hardwareMeasured.startedAt,usefulFps:hardwareMeasured.scanSummary?.usefulFps,detectorPipeline:measuredVariant?.detectorPipeline,detectorFallback:measuredVariant?.detectorFallback,sourceFormat:measuredVariant?.sourceFormat,scanFrames:measuredVariant?.scanFrames,conditions:measuredConditions.length,completeTiles:measuredConditions.reduce((n,c)=>n+c.completeTiles,0),payloadOkTiles:measuredConditions.reduce((n,c)=>n+c.payloadOkTiles,0),rawBer:measuredVariant?.metrics?.rawBer,roiMs:measuredVariant?.metrics?.averageTimings?.roiRaster,totalMs:measuredVariant?.metrics?.averageTimings?.total,persistentCameraIds:!!(hardwareMeasured.camera?.deviceId||hardwareMeasured.camera?.groupId||hardwareMeasured.cameraCapabilities?.deviceId||hardwareMeasured.cameraCapabilities?.groupId)};const byName=Object.fromEntries(report.results.map(x=>[x.name,x])),failures=[];
  if(report.physicalBaseline.build!=='v35b'||report.physicalBaseline.conditions!==6||report.physicalBaseline.usefulFps<1||report.physicalBaseline.usefulFps>2||report.physicalBaseline.workerUtilization<.9||report.physicalBaseline.m1Pitch4Frozen?.payloadOkTilesPerFrame<=0)failures.push('v35b physical diagnostic baseline changed unexpectedly');
  if(report.physicalFollowup.build!=='v35f'||report.physicalFollowup.conditions!==13||report.physicalFollowup.usefulFps<7||report.physicalFollowup.completeTiles!==436||report.physicalFollowup.payloadOkTiles!==0||report.physicalFollowup.persistentCameraIds)failures.push('v35f physical diagnostic follow-up changed unexpectedly');
  const expectedQuick=[];for(const pitch of[2,3,4])for(const profile of['binary','M1'])for(const fps of[0,5])expectedQuick.push(`${profile}-p${pitch}-f${fps}`);if(report.physicalCurrent.build!=='v35h'||report.physicalCurrent.startedAt!=='2026-08-11T13:09:51.261Z'||JSON.stringify(report.physicalCurrent.expectedConditions)!==JSON.stringify(expectedQuick)||report.physicalCurrent.conditions!==6||report.physicalCurrent.completeTiles!==117||report.physicalCurrent.payloadOkTiles!==52||report.physicalCurrent.persistentCameraIds)failures.push('v35h physical diagnostic report changed unexpectedly');if(report.physicalLatest.build!=='v35i'||report.physicalLatest.startedAt!=='2026-08-11T13:23:06.311Z'||JSON.stringify(report.physicalLatest.expectedConditions)!==JSON.stringify(expectedQuick)||report.physicalLatest.conditions!==5||report.physicalLatest.usefulFps<9||report.physicalLatest.completeTiles!==111||report.physicalLatest.payloadOkTiles!==28||report.physicalLatest.persistentCameraIds)failures.push('v35i physical diagnostic report changed unexpectedly');if(report.physicalNewest.build!=='v35j'||report.physicalNewest.startedAt!=='2026-08-11T13:32:18.988Z'||JSON.stringify(report.physicalNewest.expectedConditions)!==JSON.stringify(expectedQuick)||report.physicalNewest.detectorMode!=='sweep'||JSON.stringify(report.physicalNewest.detectorVariants)!==JSON.stringify(['d900','d768','d640'])||report.physicalNewest.conditions!==5||report.physicalNewest.usefulFps<10||report.physicalNewest.completeTiles!==106||report.physicalNewest.payloadOkTiles!==29||report.physicalNewest.persistentCameraIds)failures.push('v35j physical diagnostic report changed unexpectedly');
  if(report.physicalNative.build!=='v35l'||report.physicalNative.startedAt!=='2026-08-11T19:38:51.148Z'||report.physicalNative.usefulFps<12||report.physicalNative.detectorPipeline!=='i420'||report.physicalNative.detectorFallback||report.physicalNative.sourceFormat!=='I420'||report.physicalNative.scanFrames!==360||report.physicalNative.completeTiles!==43||report.physicalNative.payloadOkTiles!==8||report.physicalNative.persistentCameraIds)failures.push('v35l native-I420 physical report changed unexpectedly');
  if(report.physicalMeasured.build!=='v35m'||report.physicalMeasured.startedAt!=='2026-08-11T20:18:08.170Z'||report.physicalMeasured.usefulFps<7.5||report.physicalMeasured.detectorPipeline!=='i420'||report.physicalMeasured.detectorFallback||report.physicalMeasured.sourceFormat!=='I420'||report.physicalMeasured.scanFrames!==227||report.physicalMeasured.conditions!==7||report.physicalMeasured.completeTiles!==80||report.physicalMeasured.payloadOkTiles!==17||report.physicalMeasured.rawBer>.176||report.physicalMeasured.persistentCameraIds)failures.push('v35m measured marker/color-map report changed unexpectedly');
  const need=(name,minComplete,minPayload=minComplete)=>{const r=byName[name];if(!r)failures.push(`${name}: missing result`);else{if(r.complete<minComplete)failures.push(`${name}: ${r.complete} complete tiles, expected ${minComplete}`);if(r.payloads<minPayload)failures.push(`${name}: ${r.payloads} payloads, expected ${minPayload}`);}};
  need('clean-full',6,6);
  need('arbitrary-one-tile-crop',1,1);
  need('multiple-tile-crop',2,2);
  need('dense-hardware-grid',1,1);need('dense-hardware-grid-d640',20,15);need('dense-hardware-grid-i420-d768',20,15);if(byName['dense-hardware-grid-i420-d768']?.pipeline!=='i420'||byName['dense-hardware-grid-i420-d768']?.pipelineFallback||byName['dense-hardware-grid-i420-d768']?.sourceFormat!=='I420')failures.push('direct I420 detector path fell back to Canvas');if(Math.max(...(byName['dense-hardware-grid-d640']?.detector||[Infinity]))!==640)failures.push('detector long-side override was ignored');if((byName['dense-hardware-grid']?.projected||0)<=byName['dense-hardware-grid']?.verified)failures.push('dense-grid full-screen capacity projection was not reported');const denseResult=byName['dense-hardware-grid'];if(!denseResult?.neighborChecks||denseResult.neighborChecks>=denseResult.fiducials*denseResult.fiducials)failures.push('dense-grid marker neighbors fell back to an exhaustive scan');
  for(const name of['perspective','rotation-90','mirrored','blur','moire','exposure-white-balance-channel-mix'])need(name,1,1);
  need('camera-resample-p4',6,6);
  need('camera-dim-resample-p4',6,6);
  const rolling=byName['rolling-shutter-composite'];if(!rolling||rolling.frames.length<2||rolling.payloads<2)failures.push('rolling-shutter composite did not accept independent frame IDs');
  if(byName['corrupted-headers']?.complete!==0)failures.push('corrupted headers were accepted');if((byName['corrupted-headers']?.timings?.roiRaster||0)>.5)failures.push('header failures triggered full-resolution ROI reads');if(!byName['corrupted-headers']?.headerTimingPhases||byName['corrupted-headers'].headerFullPhases*20>byName['corrupted-headers'].headerTimingPhases)failures.push('known timing chips did not reject at least 95% of corrupted header phases before full BCH sampling');
  const badPayload=byName['corrupted-payloads'];if(!badPayload||badPayload.complete<1||badPayload.payloads!==0)failures.push('corrupted payloads were not independently CRC-rejected');
  if(byName['false-positive-noise']?.complete!==0)failures.push('false-positive noise was accepted');
  for(const r of report.profileResults)if(r.complete!==1||r.payloads!==1)failures.push(`${r.name}: clean profile did not round-trip`);
  if(!report.tracking.second.tracked||report.tracking.second.payloads<1)failures.push('persistent marker tracking did not decode the following frame');
  const tagged=report.hardwareTag?.tiles?.[0],expectedFlags=0x80|(2<<4)|(5<<1)|1;if(!tagged||tagged.flags!==expectedFlags||report.hardwareTag.payloads!==1)failures.push('hardware condition flags did not round-trip');
  if(report.labSmoke?.workerBuild!=='v35n'||!report.labSmoke?.versionText.includes('v35n')||!report.labSmoke.versionText.includes('hardware lab')||!report.labSmoke?.autoSender||!report.labSmoke?.autoReceiver||!report.labSmoke?.recordQuick||!report.labSmoke?.saveVideo||!report.labSmoke?.exportJson||!report.labSmoke?.exportCsv||!report.labSmoke?.staleResultsCleared||!report.labSmoke?.quickSweepText.includes('12-condition binary/M1')||report.labSmoke?.quickDwell!=='2'||report.labSmoke?.detectorMode!=='768'||JSON.stringify(report.labSmoke?.detectorOptions)!==JSON.stringify(['sweep','900','768','640'])||report.labSmoke?.pipelineMode!=='i420'||JSON.stringify(report.labSmoke?.pipelineOptions)!==JSON.stringify(['i420','canvas'])||!report.labSmoke.receiverVisible||!report.labSmoke.recordVisible||!report.labSmoke.senderHidden)failures.push('automated hardware lab receiver UI did not initialize');
  if(report.format?.version!==2)failures.push('X1 format version did not advance for the measured M1 color remap');
  if(!report.selftest?.legacyHeader)failures.push('version-1 recording compatibility failed');
  if(!report.selftest?.headerRecovered)failures.push('protected header reconstruction failed');
  if(!report.selftest?.eccRecovered)failures.push('LDPC soft reconstruction failed');
  if(!report.selftest?.fountainRecovered)failures.push('fountain reconstruction failed');
  console.log(JSON.stringify(report,null,2));
  if(failures.length){console.error('\nFAILED\n- '+failures.join('\n- '));process.exitCode=1;}else console.log('\nPASS: AirGrid X1 crop-local production-worker benchmark');
  await cdp.send('Page.close');
}finally{
  cdp?.close();server.close();
  if(process.platform==='win32')spawnSync('taskkill',['/PID',String(child.pid),'/T','/F'],{stdio:'ignore'});else child.kill('SIGKILL');
  await rm(profile,{recursive:true,force:true}).catch(()=>{});
}

name: v260 parallel sender correctness

on:
  push:
    paths:
      - .github/v260_parallel_sender_patch.py
      - .github/workflows/v260-parallel-sender.yml
  workflow_dispatch:

permissions: { contents: write }

jobs:
  correctness:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Apply v260 candidate
        run: |
          set -euo pipefail
          python3 .github/v260_parallel_sender_patch.py
      - name: Syntax check
        run: |
          set -euo pipefail
          node --check send/main.js
          node --check send/render-worker.js
          node --check receive/main.js
          node --check main.js
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - name: Prepare Chrome
        run: npm install --no-save --package-lock=false playwright@1.55.0
      - name: Run existing benchmark unchanged
        run: |
          set -euo pipefail
          python3 -m http.server 8080 >/tmp/airgapper-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID" 2>/dev/null || true' EXIT
          AIRGAPPER_TRIALS=1 node benchmark/offline-runner.mjs 2>&1 | tee /tmp/v260-regression.log
      - name: Promote passing v260
        if: success()
        run: |
          set -euo pipefail
          git config user.name "AirGapper Agent"
          git config user.email "actions@github.com"
          rm -rf node_modules benchmark/offline-summary.json
          git rm .github/v260_parallel_sender_patch.py .github/workflows/v260-parallel-sender.yml
          git add send/main.js send/render-worker.js receive/main.js main.js index.html sw.js
          git commit -m "v0.5.260 parallel sender QR pages"
          git pull --rebase origin master
          git push origin HEAD:master
      - name: Record rejected v260
        if: failure()
        run: |
          set -euo pipefail
          git reset --hard HEAD
          {
            echo "v260 parallel sender rejected at $(date -u +%FT%TZ)"
            tail -n 500 /tmp/v260-regression.log 2>/dev/null || true
          } > benchmark/v260-parallel-sender-ci.log
          git config user.name "AirGapper Agent"
          git config user.email "actions@github.com"
          git rm -f .github/v260_parallel_sender_patch.py .github/workflows/v260-parallel-sender.yml || true
          git add benchmark/v260-parallel-sender-ci.log
          git commit -m "record v260 parallel sender rejection" || true
          git pull --rebase origin master
          git push origin HEAD:master
          exit 1

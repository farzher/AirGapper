name: receiver v456 once

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/receiver-v456-once.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 1
      - name: Apply receiver convergence patch
        run: python3 .github/receiver-v456.py
      - name: Commit receiver convergence
        run: |
          rm -f .github/receiver-v456.py .github/workflows/receiver-v456-once.yml
          git config user.name "AirGapper Build"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add receive/runtime.js version.js .github
          git commit -m "Converge lattice liveness and automatic optics v0.5.456"
          git push origin HEAD:master

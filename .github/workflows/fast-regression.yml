name: fast regression

on:
  push:
    paths:
      - receive/**
      - send/**
      - shared/**
      - codec/**
      - android/**
      - main.js
      - index.html
      - sw.js
      - benchmark/**
      - .github/**
  pull_request:
    branches: [master]
    paths:
      - receive/**
      - send/**
      - shared/**
      - codec/**
      - android/**
      - main.js
      - index.html
      - sw.js
      - benchmark/**
      - .github/workflows/fast-regression.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: fast-regression-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apply-native-camera-runtime:
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/fix/native-camera-runtime') ||
      (github.event_name == 'pull_request' && github.head_ref == 'fix/native-camera-runtime')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/native-camera-runtime
          fetch-depth: 1
      - name: Apply runtime patch
        run: |
          set -euo pipefail
          if ! grep -q 'APP_BUILD = "v0.5.352"' main.js; then
            python3 .github/patch-native-camera-runtime.py
          fi
          if ! grep -q 'never hide a usable native' android/app/src/main/java/com/airgapper/app/NativeCameraBridge.java; then
            python3 .github/patch-native-camera-runtime-followup.py
          fi
          git checkout -- .github/workflows/build-apk.yml
          git config user.name "AirGapper Build"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add android/app/src/main/java/com/airgapper/app/NativeCameraBridge.java \
                  android/app/src/main/java/com/airgapper/app/NativeGpuCameraReader.java \
                  android/app/build.gradle shared/native-camera.js receive/main.js \
                  index.html main.js send/main.js sw.js
          git diff --cached --check
          if git diff --cached --quiet; then
            echo 'native runtime patch already applied'
            exit 0
          fi
          git commit -m "finish native Camera2 60fps runtime handling"
          git push origin HEAD:fix/native-camera-runtime

  regression:
    needs: [apply-native-camera-runtime]
    if: always() && (needs.apply-native-camera-runtime.result == 'success' || needs.apply-native-camera-runtime.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 35
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ (github.head_ref == 'fix/native-camera-runtime' || github.ref == 'refs/heads/fix/native-camera-runtime') && 'fix/native-camera-runtime' || github.sha }}
          fetch-depth: 1

      - name: Build Android app
        run: |
          set -euo pipefail
          cd android
          ./gradlew assembleDebug

      - name: Cache codec toolchain
        uses: actions/cache@v4
        with:
          path: |
            codec/source/emsdk
            codec/source/third_party
            codec/source/build-simd
            codec/source/build-scalar
          key: airgapper-codec-emsdk-6.0.6-zxing-${{ hashFiles('codec/source/zxing-cpp.commit') }}

      - name: Build codecs
        run: |
          set -euo pipefail
          AIRGAPPER_CODEC_SIMD=1 AIRGAPPER_CODEC_OUTPUT_DIR=.. codec/source/build.sh
          AIRGAPPER_CODEC_SIMD=0 AIRGAPPER_CODEC_OUTPUT_DIR=../scalar codec/source/build.sh

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run grid regression
        run: node benchmark/grid-lattice-regression.mjs

      - name: Install browser benchmark dependency
        run: npm install --no-save --package-lock=false playwright@1.55.0

      - name: Run production regression
        run: |
          set -euo pipefail
          python3 -m http.server 8080 >/tmp/airgapper-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID" 2>/dev/null || true' EXIT
          AIRGAPPER_TRIALS=3 node benchmark/offline-runner.mjs
          node benchmark/native-y8-worker-smoke.mjs
          node benchmark/corpus-suite.mjs

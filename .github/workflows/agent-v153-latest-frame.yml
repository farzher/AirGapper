name: Agent v153 latest-frame reservoir
on:
  push:
    paths:
      - '.github/v153_patch.py'
      - '.github/workflows/agent-v153-latest-frame.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - name: Apply patch
        run: python .github/v153_patch.py
      - name: Commit patch
        run: |
          git config user.name 'AirGapper Agent'
          git config user.email 'actions@github.com'
          git add receive/main.js index.html main.js sw.js
          git commit -m 'v0.5.153 keep latest camera frame ready'
          git push origin master

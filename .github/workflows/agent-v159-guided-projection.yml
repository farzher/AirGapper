name: Agent v159 guided projection sampler
on:
  push:
    paths:
      - '.github/v159_patch.py'
      - '.github/workflows/agent-v159-guided-projection.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - name: Apply patch
        run: python .github/v159_patch.py
      - name: Rebuild SIMD codec
        run: bash vendor/decimen-codec/source/build.sh
      - name: Smoke check
        run: |
          node --check receive/main.js
          node --check receive/worker.js
          git diff --check
      - name: Commit patch
        run: |
          git config user.name 'AirGapper Agent'
          git config user.email 'actions@github.com'
          git add index.html main.js receive/main.js sw.js \
            vendor/decimen-codec/decimen_codec.js vendor/decimen-codec/decimen_codec.wasm \
            vendor/decimen-codec/source/wrapper/decimen_codec.cpp
          git commit -m 'v0.5.159 accelerate guided sampling with tracked projection'
          git push origin master
